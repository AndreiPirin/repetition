package memorize;

public class samples {

    public static void main(String[] args) {

/*
        int age = 30; //age is a name/label, aka an identifier; equal sign is called the assignment operator

        //implicit casting
        int a = 1;
        short b = 2;
        //a + b; - is gonna be an int

        int result = (int) Math.ceil(1.1F); //explicit casting

        String x = “1”;
        int y = Integer.parseInt(x) + 2;

        int result = (int) Math.round(Math.random());

        //Method chaining.chaining.chaining

        //Ternary operator: a ? b : c // String className = income > 100_000 ? “First” : “Economy”

        public class TextBox {
            public String text; //text is a field
            public void setText (String text) {} } //text is a parameter

        TestGarbage s1 = new TestGarbage();
        s1 = null;
        System.gc(); //calling on Garbage Collector

        public Employee (int baseSalary) {
            this (baseSalary, 0);}

            OUTER: for (int…
                … break OUTER;

            this(parameter 1, parameter 2); //constructor inside a constructor


        String name = “Alpha”;

        System.out.println(new Car().color); //this object can only be used once, it is lost thereafter


        a = b = c = 18;
        result:
        a = 18,
        b = 18,
        c = 18.


        if (animal instanceof Dog) {}

        ((Lion)jen).roar();


*/

    }
}
